% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glim_logic.R
\name{prob2poss_logis}
\alias{prob2poss_logis}
\title{Probability to Possibility Mapping for Logistic Regression}
\usage{
prob2poss_logis(X, y, samples, the_compared_theta)
}
\arguments{
\item{X}{Predictor matrix.}

\item{y}{Response vector.}

\item{samples}{Matrix of simulated sample coefficients.}

\item{the_compared_theta}{The theta values to compare against.}
}
\value{
A vector of mapped possibility values.
}
\description{
Probability to Possibility Mapping for Logistic Regression
}
\examples{
\dontrun{
set.seed(1)
n <- 100
X <- cbind(intercept = 1, x = rnorm(n))
y <- rbinom(n, 1, plogis(X \%*\% c(0, 1.5)))

# Simulated candidate coefficient vectors (one per column)
samples <- cbind(c(0, 1.2), c(0.1, 1.6), c(-0.1, 1.4))

# Theta value(s) to evaluate against the simulated samples
the_compared_theta <- c(0, 1.5)

prob2poss_logis(X, y, samples, the_compared_theta)
}
}
