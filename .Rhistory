} else {
xi <- xi + w(it) * g.xi
it <- it + 1
}
}
# removed a case where D = 1
qchisq(.9)
# removed a case where D = 1
qchisq(.9, 2)
# removed a case where D = 1
qchisq(.1, 2)
i <- 0
xi <- rep(1, B)
prev_xi <- 1
# finding xi
parallel <- FALSE
i <- 0
xi <- rep(1, B)
prev_xi <- 1
# finding xi
parallel <- FALSE
for (a in AA) {
i <- i + 1
m <- 100
parallel <- FALSE
xi[i] <- imvar(
log(prev_xi),
a,
pl,
mle = mle_coefs,
J = eJ,
parallel,
tol = 5e-2,
a = 5,
b = 1,
max.it = 20
)
prev_xi <- xi[i]
}
#' @export
imvar <- function(xi, alpha, pl, mle, J, parallel, tol = 1e-2, a = 1, b = 1, max.it = 25) {
# tol <- 1e-2
# a <- 1
# b <- 1
# max.it <- 25
# xi <- 1
# mle <- mle_coefs
# alpha <- .5
# J <- eJ
D <- length(mle)
# removed a case where D = 1
posts <- as.vector(J$vectors %*% sqrt(qchisq(1 - alpha, D) / abs(J$values))) # Our current best Q, and Cholesky decomp (R^1/2) (although without the scaling xi)
# These are the directions to go
# Don't I need Qsigma^-1/2 Qt? I am very confused at why we have t(J$vectors). Shouldn't we have just J$vectors as our Q matrix?
maxpl <- function(v) max(c(pl(as.vector(mle) + v), pl(as.vector(mle) - v)))
w <- function(s) a / (1 + s)**b # our weighting function, dampens over time
it <- 1
repeat {
posts.xi <- as.vector(posts * exp(xi / 2)) # Xi scales singular values (scalar for each directions). Again, we are going to evaluate this direction * scaling to see how far off.
# exp parameterization lets us avoid negative xi (so when we do sqrt(xi) we don't get imaginary)
# removed an if else that dealt with if D == 1
g.xi <- maxpl(posts.xi) - alpha
if (all(abs(g.xi) <= tol) || (it >= max.it)) {
break
} else {
xi <- xi + w(it) * g.xi
it <- it + 1
}
}
# Return the exponential version
return(exp(xi))
}
X <- X_binary
y <- y_binary
m <- 100
pl <- function(z) glim_raw(X, y, family, ll_mle_original_data, z, m, parallel)
B <- 100
AA <- seq(0.001, 0.999, length = B)
family <- "binomial"
res <- glm(y ~ X - 1, family = family)
p_i <- res$fitted.values
J <- crossprod(X, diag(p_i * (1 - p_i)) %*% X)
J <- (J + t(J)) / 2 # symmetrize to try to kill some rounding asymmetries -- Gemini's idea
eJ <- eigen(J)
# James' solution to needing to scale along a direction didn't work in my case
# can't have zeros in the eigvalues because will not be invertible
eJ$values[eJ$values < 1e-4] <- .000001
mle_coefs <- res$coefficients
i <- 0
xi <- rep(1, B)
prev_xi <- 1
# finding xi
parallel <- FALSE
for (a in AA) {
i <- i + 1
m <- 100
parallel <- FALSE
xi[i] <- imvar(
log(prev_xi),
a,
pl,
mle = mle_coefs,
J = eJ,
parallel,
tol = 5e-2,
a = 5,
b = 1,
max.it = 20
)
prev_xi <- xi[i]
}
log(1)
imvar(log(1), .05, mle_coefs, eJ, parallel, 5e-2, 5, 1, 20)
eJ
imvar(log(1), .05, pl, mle_coefs, eJ, parallel, 5e-2, 5, 1, 20)
imvar(log(2), .05, pl, mle_coefs, eJ, parallel, 5e-2, 5, 1, 20)
tol <- 5e-2
a <- 5
b <- 1
max.it <- 20
xi <- 0
mle <- mle_coefs
alpha <- .05
J <- eJ
D <- length(mle)
# removed a case where D = 1
posts <- as.vector(J$vectors %*% sqrt(qchisq(1 - alpha, D) / abs(J$values))) # Our current best Q, and Cholesky decomp (R^1/2) (although without the scaling xi)
# These are the directions to go
# Don't I need Qsigma^-1/2 Qt? I am very confused at why we have t(J$vectors). Shouldn't we have just J$vectors as our Q matrix?
maxpl <- function(v) max(c(pl(as.vector(mle) + v), pl(as.vector(mle) - v)))
w <- function(s) a / (1 + s)**b # our weighting function, dampens over time
it <- 1
repeat {
posts.xi <- as.vector(posts * exp(xi / 2)) # Xi scales singular values (scalar for each directions). Again, we are going to evaluate this direction * scaling to see how far off.
# exp parameterization lets us avoid negative xi (so when we do sqrt(xi) we don't get imaginary)
# removed an if else that dealt with if D == 1
g.xi <- maxpl(posts.xi) - alpha
if (all(abs(g.xi) <= tol) || (it >= max.it)) {
break
} else {
xi <- xi + w(it) * g.xi
it <- it + 1
}
}
maxpl(posts.xi)
posts.xi
# removed a case where D = 1
posts <- as.vector(J$vectors %*% sqrt(qchisq(1 - alpha, D) / abs(J$values))) # Our current best Q, and Cholesky decomp (R^1/2) (although without the scaling xi)
# These are the directions to go
# Don't I need Qsigma^-1/2 Qt? I am very confused at why we have t(J$vectors). Shouldn't we have just J$vectors as our Q matrix?
maxpl <- function(v) max(c(pl(as.vector(mle) + v), pl(as.vector(mle) - v)))
w <- function(s) a / (1 + s)**b # our weighting function, dampens over time
it <- 1
repeat {
posts.xi <- as.vector(posts * exp(xi / 2)) # Xi scales singular values (scalar for each directions). Again, we are going to evaluate this direction * scaling to see how far off.
# exp parameterization lets us avoid negative xi (so when we do sqrt(xi) we don't get imaginary)
# removed an if else that dealt with if D == 1
g.xi <- maxpl(posts.xi) - alpha
print(it)
if (all(abs(g.xi) <= tol) || (it >= max.it)) {
break
} else {
xi <- xi + w(it) * g.xi
it <- it + 1
}
}
xi
w(it)
g.xi
xi <- xi + w(it) * g.xi
xi
posts.xi <- as.vector(posts * exp(xi / 2)) # Xi scales singular values (scalar for each directions). Again, we are going to evaluate this direction * scaling to see how far off.
# exp parameterization lets us avoid negative xi (so when we do sqrt(xi) we don't get imaginary)
# removed an if else that dealt with if D == 1
g.xi <- maxpl(posts.xi) - alpha
print(it)
xi
w(it)
g.xi
xi <- xi + w(it) * g.xi
it <- it + 1
posts.xi <- as.vector(posts * exp(xi / 2)) # Xi scales singular values (scalar for each directions). Again, we are going to evaluate this direction * scaling to see how far off.
# exp parameterization lets us avoid negative xi (so when we do sqrt(xi) we don't get imaginary)
# removed an if else that dealt with if D == 1
g.xi <- maxpl(posts.xi) - alpha
print(it)
xi <- xi + w(it) * g.xi
it <- it + 1
# removed a case where D = 1
posts <- as.vector(J$vectors %*% sqrt(qchisq(1 - alpha, D) / abs(J$values))) # Our current best Q, and Cholesky decomp (R^1/2) (although without the scaling xi)
# These are the directions to go
# Don't I need Qsigma^-1/2 Qt? I am very confused at why we have t(J$vectors). Shouldn't we have just J$vectors as our Q matrix?
maxpl <- function(v) max(c(pl(as.vector(mle) + v), pl(as.vector(mle) - v)))
w <- function(s) a / (1 + s)**b # our weighting function, dampens over time
it <- 1
repeat {
posts.xi <- as.vector(posts * exp(xi / 2)) # Xi scales singular values (scalar for each directions). Again, we are going to evaluate this direction * scaling to see how far off.
# exp parameterization lets us avoid negative xi (so when we do sqrt(xi) we don't get imaginary)
# removed an if else that dealt with if D == 1
g.xi <- maxpl(posts.xi) - alpha
if (all(abs(g.xi) <= tol) || (it >= max.it)) {
print("here")
print(it)
break
} else {
xi <- xi + w(it) * g.xi
it <- it + 1
}
}
all(abs(g.xi)
abs(g.xi)
abs(g.xi)
# exp parameterization lets us avoid negative xi (so when we do sqrt(xi) we don't get imaginary)
# removed an if else that dealt with if D == 1
g.xi <- maxpl(posts.xi) - alpha
tol
tol <- 1e-2
# removed a case where D = 1
posts <- as.vector(J$vectors %*% sqrt(qchisq(1 - alpha, D) / abs(J$values))) # Our current best Q, and Cholesky decomp (R^1/2) (although without the scaling xi)
it <- 1
repeat {
posts.xi <- as.vector(posts * exp(xi / 2)) # Xi scales singular values (scalar for each directions). Again, we are going to evaluate this direction * scaling to see how far off.
# exp parameterization lets us avoid negative xi (so when we do sqrt(xi) we don't get imaginary)
# removed an if else that dealt with if D == 1
g.xi <- maxpl(posts.xi) - alpha
if (all(abs(g.xi) <= tol) || (it >= max.it)) {
print("here")
print(it)
break
} else {
xi <- xi + w(it) * g.xi
it <- it + 1
}
}
exp(xi)
tol <- 1e-2
a <- 5
b <- 1
max.it <- 20
xi <- 0
mle <- mle_coefs
alpha <- .05
J <- eJ
D <- length(mle)
# removed a case where D = 1
posts <- as.vector(J$vectors %*% sqrt(qchisq(1 - alpha, D) / abs(J$values))) # Our current best Q, and Cholesky decomp (R^1/2) (although without the scaling xi)
# These are the directions to go
# Don't I need Qsigma^-1/2 Qt? I am very confused at why we have t(J$vectors). Shouldn't we have just J$vectors as our Q matrix?
maxpl <- function(v) max(c(pl(as.vector(mle) + v), pl(as.vector(mle) - v)))
w <- function(s) a / (1 + s)**b # our weighting function, dampens over time
it <- 1
repeat {
posts.xi <- as.vector(posts * exp(xi / 2)) # Xi scales singular values (scalar for each directions). Again, we are going to evaluate this direction * scaling to see how far off.
# exp parameterization lets us avoid negative xi (so when we do sqrt(xi) we don't get imaginary)
# removed an if else that dealt with if D == 1
g.xi <- maxpl(posts.xi) - alpha
if (all(abs(g.xi) <= tol) || (it >= max.it)) {
print("here")
print(it)
break
} else {
xi <- xi + w(it) * g.xi
it <- it + 1
}
}
tol <- 1e-2
a <- 5
b <- 1
max.it <- 20
xi <- 0
mle <- mle_coefs
alpha <- .4
J <- eJ
D <- length(mle)
# removed a case where D = 1
posts <- as.vector(J$vectors %*% sqrt(qchisq(1 - alpha, D) / abs(J$values))) # Our current best Q, and Cholesky decomp (R^1/2) (although without the scaling xi)
# These are the directions to go
# Don't I need Qsigma^-1/2 Qt? I am very confused at why we have t(J$vectors). Shouldn't we have just J$vectors as our Q matrix?
maxpl <- function(v) max(c(pl(as.vector(mle) + v), pl(as.vector(mle) - v)))
w <- function(s) a / (1 + s)**b # our weighting function, dampens over time
it <- 1
repeat {
posts.xi <- as.vector(posts * exp(xi / 2)) # Xi scales singular values (scalar for each directions). Again, we are going to evaluate this direction * scaling to see how far off.
# exp parameterization lets us avoid negative xi (so when we do sqrt(xi) we don't get imaginary)
# removed an if else that dealt with if D == 1
g.xi <- maxpl(posts.xi) - alpha
if (all(abs(g.xi) <= tol) || (it >= max.it)) {
print("here")
print(it)
break
} else {
xi <- xi + w(it) * g.xi
it <- it + 1
}
}
exp(xi)
#' @export
imvar <- function(xi, alpha, pl, mle, J, parallel, tol = 1e-2, a = 1, b = 1, max.it = 25) {
# tol <- 1e-2
# a <- 5
# b <- 1
# max.it <- 20
# xi <- 0
# mle <- mle_coefs
# alpha <- .4
# J <- eJ
D <- length(mle)
# removed a case where D = 1
posts <- as.vector(J$vectors %*% sqrt(qchisq(1 - alpha, D) / abs(J$values))) # Our current best Q, and Cholesky decomp (R^1/2) (although without the scaling xi)
# These are the directions to go
# Don't I need Qsigma^-1/2 Qt? I am very confused at why we have t(J$vectors). Shouldn't we have just J$vectors as our Q matrix?
maxpl <- function(v) max(c(pl(as.vector(mle) + v), pl(as.vector(mle) - v)))
w <- function(s) a / (1 + s)**b # our weighting function, dampens over time
it <- 1
repeat {
posts.xi <- as.vector(posts * exp(xi / 2)) # Xi scales singular values (scalar for each directions). Again, we are going to evaluate this direction * scaling to see how far off.
# exp parameterization lets us avoid negative xi (so when we do sqrt(xi) we don't get imaginary)
# removed an if else that dealt with if D == 1
g.xi <- maxpl(posts.xi) - alpha
if (all(abs(g.xi) <= tol) || (it >= max.it)) {
break
} else {
xi <- xi + w(it) * g.xi
it <- it + 1
}
}
# Return the exponential version
return(exp(xi))
}
X <- X_binary
y <- y_binary
m <- 100
pl <- function(z) glim_raw(X, y, family, ll_mle_original_data, z, m, parallel)
B <- 100
AA <- seq(0.001, 0.999, length = B)
family <- "binomial"
res <- glm(y ~ X - 1, family = family)
p_i <- res$fitted.values
J <- crossprod(X, diag(p_i * (1 - p_i)) %*% X)
J <- (J + t(J)) / 2 # symmetrize to try to kill some rounding asymmetries -- Gemini's idea
eJ <- eigen(J)
# James' solution to needing to scale along a direction didn't work in my case
# can't have zeros in the eigvalues because will not be invertible
eJ$values[eJ$values < 1e-4] <- .000001
mle_coefs <- res$coefficients
i <- 0
xi <- rep(1, B)
prev_xi <- 1
# finding xi
parallel <- FALSE
for (a in AA) {
i <- i + 1
m <- 100
parallel <- FALSE
imvar(log(1), .05, pl, mle_coefs, eJ, parallel, 5e-2, 5, 1, 20)
xi[i] <- imvar(
log(prev_xi),
a,
pl,
mle = mle_coefs,
J = eJ,
parallel,
tol = 5e-2,
a = 5,
b = 1,
max.it = 20
)
prev_xi <- xi[i]
}
imvar(log(1), .05, pl, mle_coefs, eJ, parallel, 5e-2, 5, 1, 20)
imvar(log(1), .05, pl, mle_coefs, eJ, parallel, 1e-2, 5, 1, 20)
i <- 0
xi <- rep(1, B)
prev_xi <- 1
# finding xi
parallel <- FALSE
for (a in AA) {
i <- i + 1
m <- 100
parallel <- FALSE
# imvar(log(1), .05, pl, mle_coefs, eJ, parallel, 1e-2, 5, 1, 20)
xi[i] <- imvar(
log(prev_xi),
a,
pl,
mle = mle_coefs,
J = eJ,
parallel,
tol = 1e-2,
a = 5,
b = 1,
max.it = 20
)
prev_xi <- xi[i]
}
U <- runif(m)
lerped_xi <- -1
samples <- matrix(nrow = length(mle_coefs), ncol = m)
i <- 0
for (u in U) {
i <- i + 1
if (u < min(AA)) {
lerped_xi <- xi[1]
} else if (u > max(AA)) {
lerped_xi <- xi[B]
} else {
r <- sum(AA < u)
w <- (u - AA[r]) / (AA[r + 1] - AA[r])
lerped_xi <- (1 - w) * xi[r] + w * xi[r + 1]
}
rand_dir <- generate_unit_matrix(1, length(mle_coefs))
spatial_dir <- eJ$vectors %*% (sqrt(1 / eJ$values) * rand_dir)
samples[, i] <- mle_coefs +
as.vector(sqrt(qchisq(1 - u, length(mle_coefs))) * lerped_xi * spatial_dir)
}
View(samples)
# )
#
# z_no_approx_matrix <- matrix(
#   out_no_approx,
#   nrow = length(beta_1),
#   ncol = length(beta_0)
# )
# dev.new()
# contour(x = beta_1, y = beta_0, z = z_no_approx_matrix)
#
z <- prob2poss_logis(X_binary, y_binary, samples, t(as.matrix(betas)))
z_matrix <- matrix(z, nrow = length(beta_1), ncol = length(beta_0))
dev.new()
contour(x = beta_1, y = beta_0, z = z_matrix)
devtools::document()
devtools::install()
# Implementation of GLMs in Ryan Martin's Inferential Model framework.
library(IMMC)
# Data setup
# y <- rbinom(40, 1, 0.5)
# X <- matrix(rnorm(2 * 40), nrow = 40)
# m <- 1000
#
# beta_1 <- seq(-2, 2, by = 0.1)
# beta_2 <- seq(-2, 2, by = 0.1)
# betas <- as.matrix(expand.grid(beta_1, beta_2))
#
# plaus <- glim(X,y,"binomial", betas, m=1000, parallel=TRUE, approx=FALSE)
#
# dev.new()
# contour(beta_1, beta_2, matrix(plaus, nrow = length(beta_1)))
X <- matrix(
c(
.0794,
.1,
.1259,
.1413,
.15,
.1558,
.1778,
.1995,
.2239,
.2512,
.2818,
.3162
),
ncol = 1
)
successes <- c(1, 2, 1, 0, 1, 2, 4, 6, 4, 5, 5, 8)
failures <- 10 - successes
y <- cbind(successes, failures) # from ?glm
# do we implement this binomial functionality? ^
# rep(vector, other_vector) will repeat each element of the vector the corresponding other_vector element no of times
X_binary <- rep(X, times = successes)
X_binary <- rbind(as.matrix(X_binary), as.matrix(rep(X, times = failures)))
X_binary <- cbind(X_binary, rep(1, sum(successes + failures)))
y_binary <- c(rep(1, times = sum(successes)), rep(0, times = sum(failures)))
beta_0 <- seq(from = -5, to = -1, by = .1)
beta_1 <- seq(0, 25, by = .2)
betas <- expand.grid(beta_1, beta_0)
fit <- glm(y_binary ~ X_binary, family = "binomial")
ll_mle_original_data <- logLik(fit)
mle_coefs <- fit$coefficients
samples <- glim(
X_binary,
y_binary,
family = "binomial",
betas,
m = 100,
parallel = TRUE,
approx = TRUE
)
# out_no_approx <- glim(
#   X_binary,
#   y_binary,
#   family = "binomial",
#   betas,
#   m = 1000,
#   parallel = TRUE,
#   approx = FALSE
# )
#
# z_no_approx_matrix <- matrix(
#   out_no_approx,
#   nrow = length(beta_1),
#   ncol = length(beta_0)
# )
# dev.new()
# contour(x = beta_1, y = beta_0, z = z_no_approx_matrix)
#
z <- prob2poss_logis(X_binary, y_binary, samples, t(as.matrix(betas)))
z_matrix <- matrix(z, nrow = length(beta_1), ncol = length(beta_0))
dev.new()
contour(x = beta_1, y = beta_0, z = z_matrix)
dev.new()
contour(x = beta_1, y = beta_0, z = z_matrix)
